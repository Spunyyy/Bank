<UserControl x:Class="Bank.AccountViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bank"
             mc:Ignorable="d" 
             Height="100" Width="750">
    <Border BorderBrush="Black" BorderThickness="1" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding AccountType}" Grid.Column="0" VerticalAlignment="Center" Margin="10,-20,10,0"/>
            <TextBlock Text="{Binding AccountNumber}" Grid.Column="0" VerticalAlignment="Center" Margin="10,20,10,0" />
            <TextBlock Text="{Binding Balance, StringFormat=C}" Grid.Column="1" VerticalAlignment="Center" Margin="10" />
            <Button Content="Details" Grid.Column="2" Margin="10" Click="OnDetailsClick" />
        </Grid>
    </Border>
</UserControl>
